# Nano Banana + Milvus 电商 AI 生图流水线

> 基于向量检索和 AIGC 的电商宣传图自动生成系统

---

## 项目背景

### 痛点问题

中小电商企业在商品宣传图制作中面临以下挑战：

- **成本高**：专业拍摄 + 后期修图，单张成本可达几十至上百元
- **周期长**：从新品上架到宣传图完成，通常需要数天时间
- **质量不稳定**：依赖设计师个人能力，风格难以统一

### 解决方案

本项目通过 **AI 向量检索 + 循环检索 + 查询重写 + 风格分析 + 图像生成 + 质量评估** 技术��现：

```
新品平铺图 → 循环检索相似爆款 → 提取拍摄风格 → AI 生成宣传图 → AI 质量评分
```

**核心价值**：
- 无需本地 GPU，所有模型通过 API 调用
- 端到端自动化，单张图片生成成本约 $0.014 (FLUX.2 Klein)
- 基于历史爆款数据，保证生成风格符合品牌调性
- **循环检索状态机**：自动优化检索结果，最多3轮迭代
- **查询重写**：根据质量评分自动调整检索条件
- **本地缓存**：相同爆款组合自动复用风格分析结果，节省 API 谹用
- AI 自动质量评估，确保生成图片可用性

### 第一性原则

**服装必须来自用户上传的图片**：系统生成的宣传图中，模特身上穿的服装必须是用户上传的原款服装。参考爆款图只用于提取**拍摄风格**（场景、光线、构图、氛围），不提供服装款式参考。

---

## 技术栈

| 类别 | 技术 | ���途 |
|------|------|------|
| **向量数据库** | Milvus 2.4+ | 混合向量检索（Dense + Sparse + Scalar） |
| **Embedding 模型** | nvidia/llama-nemotron-embed-vl-1b-v2 | 图文向量化（2048维）|
| **LLM 模型** | qwen/qwen3-vl-235b-a22b-thinking | 视觉风格分析 + 质量评估 |
| **图像生成** | black-forest-labs/flux.2-klein-4b | 宣传图生成 |
| **API 聚合** | OpenRouter | 统一模型调用接口 |
| **文本处理** | scikit-learn (TF-IDF) | 稀疏向量生成 |
| **图像处理** | Pillow | 图片加载、格式转换 |
| **后端框架** | FastAPI | RESTful API 服务 |
| **前端框架** | Vue 3 + Vite | Web 界面 |

---

## 架构设计

### 核心设计理念：第一性原则

**服装必须来自用户上传的图片**：系统生成的宣传图中，模特身上穿的服装必须是用户上传的原款服装。参考爆款图只用于提取**拍摄风格**（场景、光线、构图、氛围），绝不提供服装款式参考。

### 数据流向图

```
                    ┌─────────────────┐
                    │   用户上传       │
                    │   新品服装图     │
                    └────────┬────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        ┌───────────────┐         ┌───────────────┐
        │  向量化编码    │         │   元数据提取    │
        │  Dense+Sparse │         │  品类/风格/季节 │
        └───────┬───────┘         └───────┬───────┘
                │                         │
                └────────────┬────────────┘
                             ▼
                    ┌──────────────────┐
                    │  【循环检索状态机】│
                    │  第1轮: 原始条件  │
                    │  第2轮: 查询重写  │
                    │  第3轮: 最大化召回│
                    │   Milvus 混合    │
                    │   向量检索       │
                    │   (Dense+Sparse) │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │  质量评分 (≥7分) │
                    │  ┌─────────────┐ │
                    │  │ 品类匹配度   │ │
                    │  │ 风格匹配度   │ │
                    │  │ 场景匹配度   │ │
                    │  │ 属性匹配度   │ │
                    │  └─────────────┘ │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │  返回相似爆款图  │
                    │  (1-6张，去重)   │
                    └────────┬─────────┘
                             │
                ┌────────────┴────────────┐
                │  【关键分离点】          │
                │  ┌─────┴─────┐          │
                │  │           │          │
                ▼  ▼           ▼          ▼
        ┌──────────┐   ┌──────────────────┐
        │ 爆款参考图 │   │  LLM 风格分析   │
        │ (仅用于   │──▶│  + 缓存层        │
        │ 风格分析)  │   │                  │
        └──────────┘   │ 输入：参考图     │
                        │ 检查：缓存键     │
                        │   ├─ 命中 → 返回 │
                        │   └─ 未命中 → LLM│
                        │       Qwen3-VL   │
                        │ 输出：拍摄风格     │
                        └───────┬───────────┘
                                │
                                ▼
                        ┌───────────────┐
                        │  图像生成模型  │
                        │  FLUX.2 Klein │
                        │  输入：        │
                        │  ├ 用户服装图  │
                        │  └ 风格(文字)  │
                        │  ⚠️ 不传入参考图│
                        └───────┬───────┘
                                │
                                ▼
                        ┌───────────────┐
                        │   生成宣传图   │
                        │ 服装=用户原图  │
                        │ 风格=爆款风格  │
                        └───────┬───────┘
                                │
                        ┌───────┴───────┐
                        │               │
                        ▼               ▼
                 ┌──────────┐    ┌──────────┐
                 │ 可选：    │    │  输出：   │
                 │ AI质量评估│    │+ 保存缓存│
                 └──────────┘    └──────────┘
```

### 循环检索状态机

系统采用智能循环检索策略，确保获取最佳参考商品：

```
┌─────────────────────────────────────────────────────────────┐
│                    循环检索状态机                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐    评分≥7分?    ┌─────────┐                   │
│  │ 第1轮检索 │──────────────▶│ 返回结果 │                   │
│  │ 原始条件  │   No           └─────────┘                   │
│  └────┬────┘    │                                         │
│       │         ▼                                         │
│       │    ┌─────────┐    评分≥7分?    ┌─────────┐         │
│       │    │ 第2轮检索 │──────────────▶│ 返回结果 │         │
│       │    │ 查询重写1  │   No           └─────────┘         │
│       │    └────┬────┘    │                                 │
│       │         │          ▼                                 │
│       │         │    ┌─────────┐                            │
│       │         │    │ 第3轮检索 │                           │
│       │         │    │ 查询重写2  │                           │
│       │         │    └────┬────┘                            │
│       │         │         │                                  │
│       ▼         ▼         ▼                                  │
│  ┌─────────────────────────────────┐                        │
│  │        返回最佳结果              │                        │
│  └─────────────────────────────────┘                        │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  查询重写规则：                                               │
│  • 第1次重写：品类→父类，销量阈值 1500→1000                  │
│  • 第2次重写：去掉品类限制，销量阈值 →500                     │
├─────────────────────────────────────────────────────────────┤
│  质量评分维度：                                               │
│  • 品类匹配度 (0-10分)                                       │
│  • 风格匹配度 (0-10分)                                       │
│  • 场景匹配度 (0-10分)                                       │
│  • 属性匹配度 (0-10分)                                       │
└─────────────────────────────────────────────────────────────┘
```

### 缓存工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     风格分析缓存工作流                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  输入：参考爆款图 (1-6张)                                         │
│           │                                                       │
│           ▼                                                       │
│  ┌─────────────────────┐                                        │
│  │  生成缓存键           │                                        │
│  │  cache_key = MD5(    │                                        │
│  │    所有参考图base64)  │                                        │
│  └──────────┬──────────┘                                        │
│             │                                                     │
│             ▼                                                     │
│  ┌─────────────────────┐                                        │
│  │  检查本地缓存         │  backend/cache/*.json              │
│  │  ┌───────────────┐   │                                        │
│  │  │ 缓存文件存在？ │   │                                        │
│  │  └───────┬───────┘   │                                        │
│  │          │           │                                        │
│  │      ┌───┴───┐       │                                        │
│  │      │       │       │                                        │
│  │     YES      NO      │                                        │
│  │      │       │       │                                        │
│  │      ▼       ▼       │                                        │
│  │  ┌─────┐ ┌─────────┐│                                        │
│  │  │直接 │ │调用LLM ││                                        │
│  │  │返回 │ │分析风格││                                        │
│  │  │结果 │ │Qwen3-VL││                                        │
│  │  └─────┘ │~10-20s ││                                        │
│  │       ┌─┴───────┐ │                                        │
│  │       │保存到缓存│ │                                        │
│  │       └────────┘ │                                        │
│  │       │         │                                        │
│  └───────┴─────────┴───────────────────────────────────────┘│
│             │                                                     │
│             ▼                                                     │
│     输出：拍摄风格描述 (文字)                                   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  性能对比：                                                      │
│  • 缓存命中：~0.01秒 (跳过 LLM 调用)                              │
│  • 缓存未命中：~10-20秒 (LLM + 写缓存)                             │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  缓存文件命名格式：                                              │
│  style_analysis_a94a8fe5ccb19ba61c4c0873d391e987982fbbd3.json      │
│                                                                 │
│  缓存内容格式：                                                  │
│  {                                                              │
│    "combined_style": "极简主义风格，柔和自然光线...",                │
│    "individual_analyses": ["纯白背景", "冷色调"]                   │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

### 模块架构详解

#### 模块1：向量化 (embedding.py)

```python
# 输入：新品图片 + 元数据
# 处理：Dense向量(视觉) + Sparse向量(文本)
# 输出：2048维向量 + TF-IDF稀疏向量

Dense向量 ──┐
            ├──▶ RRF融合 ──▶ Milvus检索
Sparse向量─┘
```

#### 模块2：智能循环检索 (retrieval.py)

```python
# 输入：Dense向量 + Sparse向量 + 过滤条件 + 查询上下文
# 处理：循环检索 + 查询重写 + 质量评分 + 智能去重
# 输出：相似爆款列表(1-6张)

检索策略：
├── 第1轮: 原始条件检索
│   ├── Dense Search  ──┐
│   ├── Sparse Search ──┼──▶ RRF Ranker ──▶ 去重过滤
│   └── Scalar Filter ──┘      │
├── 质量评分 (RetrievalQualityJudge)
├── 第2轮: 查询重写 (品类→父类, 销量↓)
├── 质量评分
├── 第3轮: 查询重写 (去掉品类限制)
└── 质量评分 ──▶ 返回最佳结果
```

#### 模块3：拍摄风格分析 (image_gen.py)

```python
# 输入：爆款参考图(1-6张)
# 模型：Qwen3-VL
# 输出：拍摄风格描述(文字)

# ⚠️ 重要：只分析拍摄风格，不描述服装款式！
风格分析维度：
├── 场景/背景设置    # 如：纯白背景、自然户外
├── 光线和色调      # 如：柔和自然光、暖色调
├── 模特姿势和构图   # 如：全身站姿、动态抓拍
└── 整体氛围和美学   # 如：极简主义、商务专业

# ❌ 不提取：
#    × 服装款式
#    × 服装颜色
#    × 服装材质
```

#### 模块4：图像生成 (image_gen.py)

```python
# 输入：用户服装图 + 拍摄风格(文字)
# 模型：FLUX.2 Klein
# 输出：宣传图(服装=用户原图)

# 【第一性原则】生成模型输入：
gen_content = [
    {"image": 用户上传的服装图},     # ✅ 传入
    # ❌ 不传入参考图！避免服装款式被参考
    {"text": """
        【第一性原则】模特必须穿着第一张图片的服装！
        拍摄风格参考：{style_prompt}
        场景提示：{scene_hint}
    """}
]
```

#### 模块5：质量评估 (image_gen.py)

```python
# 输入：新品图 + 生成图 + 参考爆款图
# 模型：Qwen3-VL
# 输出：多维度评分(1-5分)

评分维度：
├── clothing_accuracy   # 服装准确性（第一性原则核心指标）
├── pose_naturalness    # 姿势自然度
├── scene_quality       # 场景质量
├── lighting_quality    # 布光质量
└── commercial_value    # 商业价值

# 质量判断：
# - clothing_accuracy < 4 → 违反第一性原则，需重新生成
# - average < 3.5 → 建议重新生成
```

#### 模块6：本地缓存 (utils.py)

```python
# 功能：缓存 LLM 风格分析结果，减少重复 API 调用
# 存储格式：JSON 文件
# 缓存键：MD5(参考图 base64 拼接)

工作流程：
├── 生成缓存键 = MD5(所有参考图内容)
├── 检查缓存是否存在
│   ├── 存在 → 直接返回缓存结果 (~0.01秒)
│   └── 不存在 → 调用 LLM → 保存到缓存 (~10-20秒)
└── 下次相同图片组合直接命中缓存

# 缓存目录：backend/cache/
# 配置开关：ENABLE_CACHE (默认启用)
```

**缓存效果**：
- 相同爆款组合重复生成时，跳过 LLM 调用
- 节省 API 费用，提升响应速度
- 支持按前缀清理（如只清理风格分析缓存）

### 智能检索特性

**去重与过滤机制**：
- **相关性过滤**：只返回相似度高于阈值的结果
- **颜色去重**：不返回相同颜色的产品
- **同款去重**：不返回同款不同色的产品
- **可变数量**：返回 1-6 张参考图，取决于实际符合条件的数量

**RRF 融合公式**：`score(d) = Σ 1 / (k + rank_i(d))`

---

## 项目结构

```
nano-banana-milvus/
├── backend/              # FastAPI 后端
│   ├── api.py           # API 服务
│   ├── config.py        # 配置文件
│   ├── embedding.py     # 向量嵌入
│   ├── retrieval.py     # 检索模块（循环检索+查询重写）
│   ├── image_gen.py     # 图像生成 + 质量评估 + 风格分析
│   ├── utils.py         # 工具函数 + 缓存函数
│   ├── main.py          # CLI 入口
│   ├── images/          # 历史爆款图片
│   ├── new_products/    # 新品图片
│   ├── output/          # 生成结果
│   ├── cache/           # 本地缓存目录（风格分析等）
│   ├── products.csv     # 商品元数据
│   └── requirements.txt # Python 依赖
│
└── frontend/            # Vue3 前端
    ├── index.html       # 入口文件
    ├── package.json     # 依赖配置
    ├── vite.config.js   # Vite 配置
    └── src/
        ├── main.js
        ├── App.vue
        └── components/
            ├── UploadForm.vue      # 上传表单（含高级选项）
            └── ResultsGallery.vue   # 结果展示（含质量评分）
```

### 核心模块说明

| 文件 | 作用 | 核心类/函数 |
|------|------|------------|
| `api.py` | FastAPI 服务 | `/api/upload`, `/api/tasks/{task_id}` |
| `retrieval.py` | 循环检索+查询重写 | `retrieve_similar_bestsellers()` (状态机) |
| `retrieval.py` | 检索质量评估 | `RetrievalQualityJudge` 类 |
| `embedding.py` | 向量生成 | `EmbeddingGenerator` 类 |
| `image_gen.py` | 图像生成+评估+风格分析 | `ImageGenerator`, `ImageQualityJudge` |
| `config.py` | 配置管理 | API Key、模型 ID、路径常量、缓存配置 |
| `utils.py` | 工具函数+缓存 | `get_image_embeddings()`, `image_to_uri()`, `get_cache_key()`, `save_to_cache()`, `load_from_cache()` |

---

## 快速开始

### 环境要求

- Python 3.9+
- Node.js 16+
- Milvus 2.4+ (Docker 或本地安装)
- OpenRouter API Key

### 1. 启动 Milvus

```bash
docker run -d --name milvus-standalone \
  -p 19530:19530 \
  -v $(pwd)/backend/milvus_data:/var/lib/milvus \
  milvusdb/milvus:latest
```

### 2. 配置环境变量

创建 `.env` 文件：

```bash
OPENROUTER_API_KEY=your-key-here
```

### 3. 启动后端

```bash
cd backend
python -m venv .venv
.venv\Scripts\activate  # Windows
pip install -r requirements.txt
python api.py
```

后端运行在 http://localhost:8000

### 4. 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端运行在 http://localhost:3000

---

## 使用指南

### Web 界面使用

1. **上传新品图片**：拖拽或点击上传
2. **选择商品属性**：品类、风格、季节
3. **场景提示（可选）**：描述想要的场景
4. **高级选项**：
   - 启用 AI 质量评估
   - 选择裁判模型（可选）
   - 启用后将生成图片并进行多维度评分

### API 调用

**上传并生成**：
```bash
curl -X POST "http://localhost:8000/api/upload" \
  -F "file=@new_product.jpg" \
  -F "category=midi_dress" \
  -F "style=elegant" \
  -F "season=summer" \
  -F "enable_quality_check=true" \
  -F "judge_model=qwen/qwen3-vl-235b-a22b-thinking"
```

**查询任务状态**：
```bash
curl "http://localhost:8000/api/tasks/{task_id}"
```

### API 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/upload` | POST | 上传并生成 |
| `/api/tasks/{task_id}` | GET | 任务状态轮询 |
| `/api/output/{product_id}/{filename}` | GET | 获取生成图片 |
| `/api/categories` | GET | 品类列表 |
| `/api/styles` | GET | 风格列表 |

### 高级选项参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `enable_quality_check` | boolean | false | 是否启用 AI 质量评估 |
| `judge_model` | string | "" | 裁判模型（空则使用默认模型）|
| `scene_hint` | string | "" | 场景提示描述 |

---

## 工作流程日志

系统运行时会输出详细的阶段日志：

```
╔══════════════════════════════════════════════════════════╗
║              电商 AI 生图流水线                            ║
╠══════════════════════════════════════════════════════════╣
║  新品ID: NEW_1234567890_abcd1234                         ║
╚══════════════════════════════════════════════════════════╝

[1/6] 初始化流水线组件...
[2/6] 加载新品图片...
      ✓ 图片加载成功: 1024x1024
      品类: midi_dress | 风格: elegant | 季节: summer

[3/6] 编码新品向量 (Dense + Sparse)...
      ✓ Dense向量: 2048维
      ✓ Sparse向量: 15个非零项

[4/6] 检索相似爆款...

════════════════════════════════════════════════════════════
  【循环检索状态机】已启用
════════════════════════════════════════════════════════════
  查询条件: 品类: midi_dress | 风格: elegant | 季节: summer
  目标数量: 3 | 最大结果: 6
  质量阈值: 7.0/10 | 最大轮次: 3
════════════════════════════════════════════════════════════

════════════════════════════════════════════════════════════
  第 1 轮检索
════════════════════════════════════════════════════════════
  检索策略: 原始条件
  过滤条件: category='midi_dress', sales_count > 1500
  ───────────────────────────────────────────────────────────
  ✓ 检索到 3 个结果

  【质量评估】
    总分: 8.5/10 [████████░░]
      ├─ 品类匹配: 9/10
      ├─ 风格匹配: 8/10
      ├─ 场景匹配: 8/10
      └─ 属性匹配: 9/10
    ⭐ 已更新为最佳结果

  ✓✓✓ 质量达标（≥7.0），提前返回第 1 轮结果

[5/6] 分析风格并生成宣传图...
      模式: 标准模式 (FLUX.2 Klein)

[6/6] 保存结果文件...
      ✓ 生成图片: 1 张
      ✓ 保存参考图: 3 张

╔══════════════════════════════════════════════════════════╗
║                     任务完成!                              ║
╚══════════════════════════════════════════════════════════╝
```

---

## AI 质量评估

### 检索质量评估

循环检索过程中，系统会自动评估检索结果质量：

| 评分维度 | 说明 | 满分 |
|---------|------|------|
| `category_match` | 品类匹配度 | 10 |
| `style_match` | 风格匹配度 | 10 |
| `scene_match` | 场景匹配度 | 10 |
| `attribute_match` | 属性匹配度 | 10 |
| `average` | 平均分 | 10 |

**检索质量判断**：
- 平均分 ≥ 7：检索质量合格，提前返回
- 平均分 < 7：继续下一轮检索，执行查询重写

### 生成图片质量评估

启用质量评估后，AI 裁判会对生成的图片进行多维度评分：

| 评分维度 | 说明 | 满分 |
|---------|------|------|
| `clothing_accuracy` | 服装准确性：与原图匹配度 | 5 |
| `pose_naturalness` | 姿势自然度：模特姿势和合身度 | 5 |
| `scene_quality` | 场景质量：背景/场景专业性 | 5 |
| `lighting_quality` | 布光质量：光线质量 | 5 |
| `commercial_value` | 商业价值：是否适合电商使用 | 5 |
| `average` | 平均分 | 5 |

**质量判断**：
- 平均分 ≥ 3.5：质量合格
- 平均分 < 3.5：建议重新生成
- 服装准确性 < 4：建议重新生成（违反第一性原则）

---

## 配置说明

### 模型配置 (`config.py`)

```python
# API 配置
OPENROUTER_API_KEY = "your-key-here"

# 模型选择
LLM_MODEL = "qwen/qwen3-vl-235b-a22b-thinking"      # LLM（风格分析+质量评估）
LIGHT_LLM_MODEL = "qwen/qwen3-vl-235b-a22b-thinking"  # 轻量级LLM（检索质量评估）
IMAGE_GEN_MODEL = "black-forest-labs/flux.2-klein-4b"  # 图像生成
EMBED_MODEL = "nvidia/llama-nemotron-embed-vl-1b-v2"   # 向量嵌入

# 图像生成配置
DEFAULT_ASPECT_RATIO = "3:4"  # 宽高比
DEFAULT_IMAGE_SIZE = "2K"     # 分辨率

# 检索配置
MIN_SALES_COUNT = 1500        # 最低销量阈值
MIN_SIMILARITY = 0.5          # 相似度阈值（0-1，越小越相关）
MAX_RESULTS = 6               # 最多返回参考图数量

# 超时配置
API_TIMEOUT = 300             # API 超时时间（秒）

# 模型分级配置
MODEL_TIERS = {
    "high_quality": {...},  # 高质量模式
    "standard": {...},      # 标准模式（默认）
    "economy": {...}        # 经济模式
}
CURRENT_MODEL_TIER = "standard"  # 当前层级（可通过环境变量覆盖）

# 缓存配置
CACHE_DIR = PROJECT_ROOT / "cache"    # 缓存目录
ENABLE_CACHE = True                    # 是否启用缓存（默认开启）
CACHE_MAX_SIZE_MB = 500                # 缓存目录最大大小
```

### 超时配置

系统已优化超时配置，避免大图片生成或复杂 LLM 评估时超时：

| API 类型 | 超时时间 |
|---------|---------|
| 图片嵌入 API | 300秒 (5分钟) |
| 文本嵌入 API | 180秒 (3分钟) |
| LLM 请求 | 300秒 (5分钟) |
| 图片生成请求 | 300秒 (5分钟) |

### 缓存配置

系统支持本地缓存，减少重复的 LLM 调用：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `ENABLE_CACHE` | 是否启用缓存 | `true` |
| `CACHE_DIR` | 缓存文件目录 | `backend/cache/` |
| `CACHE_MAX_SIZE_MB` | 缓存目录最大大��� | 500 MB |

**环境变量控制**：
```bash
# 禁用缓存
export ENABLE_CACHE=false

# 切换模型层级
export OVERRUN_MODEL_TIER=economy
```

**缓存管理**：
```python
# Python 代码中清理缓存
from utils import clear_cache
clear_cache()                    # 清理全部缓存
clear_cache("style_analysis")   # 只清理风格分析缓存
```

**缓存效果**：
- 相同爆款组合重复生成时：跳过 LLM 调用，响应时间从 ~15秒 降至 ~0.01秒
- 节省 API 费用：风格分析调用次数显著减少

---

## 常见问题

**Q: Milvus 连接失败？**

A: 检查 Milvus 是否启动：`docker ps | grep milvus`

**Q: API 调用报错？**

A: 检查 API Key 是否正确，访问 https://openrouter.ai/settings/credits 查看余额

**Q: 检索结果为空？**

A: 检查数据库是否已初始化，确认品类和销量设置合理。循环检索会自动放宽条件重试。

**Q: 生成的参考图很少？**

A: 智能检索会自动去重，返回数量取决于符合条件的不同产品数量。循环检索会自动优化结果。

**Q: 质量评分偏低？**

A: 检查服装准确性得分，如果低于4分说明服装与原图不匹配

**Q: 生成的服装不是原图款式？**

A: 这违反了第一性原则，请检查 `image_gen.py` 中是否正确配置了不传入参考图

**Q: API 请求超时？**

A: 系统已将超时时间增加到5分钟，如果仍超时请检查网络连接

**Q: 如何清理缓存？**

A: 缓存文件存储在 `backend/cache/` 目���中，可以通过以下方式清理：
```bash
# 直接删除缓存目录
rm -rf backend/cache/*

# 或在 Python 代码中
from utils import clear_cache
clear_cache()
```

**Q: 缓存会占用多少磁盘空间？**

A: 每个风格分析缓存文件约 1-5 KB，建议定期清理。系统设置了 500 MB 的建议上限。

---

## 性能指标

### 检索性能

基于 120 个商品的测试集：

| 检索方法 | Precision@5 | 说明 |
|---------|-------------|------|
| Dense 向量 | 76% | 仅使用视觉特征向量 |
| Sparse 向量 | 40% | 仅使用文本特征向量 |
| **混合检索 (RRF)** | **40%** | Dense + Sparse 融合 + 智能去重 |
| **循环检索** | **待测试** | 自动优化检索条件 |

**详细指标** (Dense 向量):
- **MRR**: 0.50 (第一个相关商品平均排名)
- **MAP**: 1.84 (平均准确率均值)
- **NDCG@5**: 0.96 (归一化折损累计增益)

### 成本对比

| 场景 | 传统拍摄 | AI生图 | 节省 |
|------|---------|--------|------|
| 10商品 | RMB 4,250 | RMB 3 | **99.9%** |
| 200商品/年 | RMB 27,050 | RMB 60 | **99.8%** |
| 1000商品/年 | RMB 124,800 | RMB 300 | **99.8%** |

单张成本：传统 RMB 30-100 vs AI RMB 0.03 (FLUX.2 Klein)

### 时间成本

| 项目 | 传统方式 | AI方式 |
|------|---------|--------|
| 准备周期 | 3-7天 | <5分钟 |
| 单张生成 | 30-60分钟 | 30-60秒 |

---

## 更新日志

### v2.1.0 (2026-03)

**新增功能**：
- ✨ **本地缓存系统**：风格分析结果自动缓存，相同爆款组合复用结果
- ✨ **模型分级配置**：支持 high_quality / standard / economy 三种模式
- ✨ **缓存管理函数**：`get_cache_key()`, `save_to_cache()`, `load_from_cache()`, `clear_cache()`
- 📝 **缓存工作流程**：基于 MD5 哈希的缓存键生成，自动命中检测

**优化**：
- 🚀 相同爆款组合重复生成时响应速度提升 99%+
- 💰 减少 LLM API 调用，降低运营成本
- 🔧 完善异常处理，缓存失败自动降级到正常流程

### v2.0.0 (2026-03)

**新增功能**：
- ✨ **循环检索状态机**：最多3轮迭代优化检索结果
- ✨ **查询重写**：根据质量评分自动调整检索条件
- ✨ **检索质量评估**：RetrievalQualityJudge 类
- 📝 **详细日志输出**：6阶段工作流程可视化

**优化**：
- 🚀 切换到 FLUX.2 Klein 图片生成模型（成本降低75%）
- ⏱️ 增加 API 超时时间到 5 分钟
- 🔧 完善错误处理和模型回退机制

**修复**：
- 🐛 修复图片生成与上传图片服装不一致问题
- 🐛 修复检索结果去重逻辑

---

## 许可证

本项目仅供学习参考使用。
